export const config = {
    // Target date (recurring yearly, ignoring year)
    targetMonth: 12,      // December
    targetDay: 20,        // 20th
    targetHour: 12,       // 12:00 PM
    targetMinute: 0,
    targetSecond: 0,

    // Gear configuration
    gears: {
        count: 10,
        minSize: 40,
        maxSize: 120,
        minSpeed: 10,
        maxSpeed: 40,
        outlineChance: 0.3
    }
};
